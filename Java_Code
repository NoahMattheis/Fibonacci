/* Recursive method to take in an int n and return the nth fibonacci number */
public int fibonacci(int n) {
  if (n == 0 && n - 1 < 0) {
    return 0;
  } else if (n == 1) {
    return 1;
  } else { 
    return fibonacci(n - 1) + fibonacci(n - 2);
  }
}
